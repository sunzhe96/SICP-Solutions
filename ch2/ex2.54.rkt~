#lang sicp

;; Exercise 2.54

(define (my-equal? list1 list2)
  (cond ((and (null? list1) (null? list2)) #t)
        ((eq? (car list1) (car list2))
         (my-equal? (cdr list1) (cdr (list 2))))
        (else #f)))

(equal? '(this is a list) 
        '(this is a list))
