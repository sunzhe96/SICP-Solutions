#lang sicp

;; Exercise 2.28

(define x (list (list 1 2) (list 3 4)))

(define (fringe tree)
  (cond
    ((null? tree) nil) 
    ((pair? tree)
     (append (fringe (car tree))
             (fringe (cdr tree))))
    (else (list tree))))

(fringe (list x (list x (list x x))))
