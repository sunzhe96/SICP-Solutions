#lang sicp

;; Exercise 2.21

(define (square x) (* x x))

(define (square-list items)
  (if (null? iterms)
      nil
      (cons (square (car items) (square-list (cdr items))))))

(square-list (list 1 2 3 4))
