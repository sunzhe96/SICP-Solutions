#lang sicp

;; Exercise 2.18

(define (reverse list)
  
  (define (iter new-list term)
    (if (null? term)
        new-list
        (iter (cons (car term) new-list) (cdr term))))
  
  (iter nil list))

(define a (list 1 2 3))
(reverse a)
